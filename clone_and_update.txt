No amount of careful planning will ever replace dumb luck.
Education is the path from cocky ignorance to miserable uncertainty. (Mark Twain)
a loser is not someone who fails but someone who doesn't try
maintain just enough order to be creative
the universe will do what it must

THE FOLLOWING REQUIRE ROOT PRIVILEGES
=====================================
1. clear space
   ===========
   $ /usr/bin/octopi-cachecleaner &
   $ bleachbit
   $ sudo bleachbit

2. run clone script (see group 'scripts & files')

3. system update
   =============
   Add/Remove Software with pamac (GUI):
       a. Refresh databases
       b. Updates -> install

   or

   Use command line:
       a. $ sudo pacman -Syu
       b. $ pacaur -Syu              # update AUR
       c. $ pacaur --aur-cleanall    # offer to remove all built packages
       d. $ paru -Syu --skipreview --noinstalldebug --upgrademenu --removemake --cleanafter --nokeepsrc --sudoloop --nokeeprepocache --redownload=all \
                      --rebuild=tree # check if some packages are out of date or orphans
       e. $ paru --clean             # remove unneeded dependencies
       f. $ paru --repoctl --clean   # remove packages that are not currently installed from repos

   and finally

   $ fwupdmgr update                 # update firmware
   run kodi to update kodi add ons
   run qBitTorrent and update plug ins

4. repeat the first step

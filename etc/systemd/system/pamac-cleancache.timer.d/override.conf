# remove all build files, build directory is specified in pacman.conf
[Unit]
Description=
Description=Daily clean packages cache
Requires=pacman-filesdb-refresh.timer
After=pacman-filesdb-refresh.timer

[Timer]
OnCalendar=
OnCalendar=daily
RandomizedDelaySec=1hr
